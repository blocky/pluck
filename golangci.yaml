version: "2"
linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused

formatters:
  enable:
    # - gci
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - "github.com/blocky"
        - "go.bky.sh"
